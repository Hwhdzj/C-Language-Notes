# 第三讲

## 循环语句

#### 1.while的用法

除了0，其他都是真的。

自减，i--与--i的区别。

主要判断表达式是否为真。

```
include <stdio.h>
int a = 7;
int main(void) {
    while ("a>0"); {
        printf("%d\n,", a);
        a = a - 1;
    }
    printf("OK\n");
    return 0;

```



#### 2.do语句

一定会执行一次。

#### 3.for语句

while的集合用法。

判断后续是否为真，是则继续，否则停止。

#### 4.break语句

终止循环，跳转的下面。