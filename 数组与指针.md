第09讲：数组与指针

一、数组
下标从 0 开始！arr[0] 是第一个。
数组名 arr 本质上就是首元素的地址。

```c
int arr[3] = {1, 2, 3};
```

二、指针

专门用来存“内存地址”的变量。
 两个符号：&（取地址）、*（取值/解引用）。

```c
int a = 10;
int *p = &a;  // p存了a的地址
printf("%d", *p); // 打印出来是10
```

三、数组与指针的关系（重点）

arr[i] 和 *(arr + i) 是完全等价的！
可以用指针遍历数组：

```c
int arr[3] = {10, 20, 30};
int *p = arr;
for (int i = 0; i < 3; i++) {
    printf("%d ", *(p + i)); // 输出 10 20 30
}