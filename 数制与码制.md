数制与码制

一、 数制：

十进制：平时数数，0-9，满10进1。
二进制：计算机只认识0和1，满2进1。
八进制：0-7，满8进1。数字前面加个 0 就是八进制，比如 012。
十六进制：0-9和A-F，满16进1。C语言里前面加 0x，比如 0xA 就是十进制的10。

例：

```c
int a = 10;  // 十进制
int b = 012; // 八进制
int c = 0xA; // 十六进制
printf("%d", a); // 打印出来全都是10
```

转换

十进制转二进制：一直除以2，看余数，倒着写。
二进制转十六进制：四个一组，直接拼。

二、 码制：怎么表示正负数

计算机里只有0和1，没地方写“+”、“-”号。所以规定：最高位当符号位（0是正数，1是负数）。

1. 原码

正数：比如 +5，就是 0000 0101。
负数：比如 -5，就是把最高位变成1，1000 0101。
缺点：0有两种写法（+0和-0）；而且计算机做减法特别麻烦，还得专门搞个减法电路。

2. 反码

正数：跟原码一样。
负数：符号位不动，后面的数字全部反过来（0变1，1变0）。

正数：跟原码一样。
负数：反码的基础上再加1。比如 -5 就是 1111 1011。