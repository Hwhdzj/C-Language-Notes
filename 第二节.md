# 一 表达式

#### 1.语句

#### 2. 运算符

！=表示不等于

int和float运算得到是float

#### 3.运算符优先级

和数学一样

#### 4.赋值

把右边的赋到左边

左边不动

区分b=a++;b=++a

例：

```
#include "stdio.h"
int a=5;
int b=7;
int main()
{
    b=++a;//先增后用
    printf("b=%d",b);
    return 0;
}
```

###### b=6

# 二 语句

2<3为真，判定为1

2>3为假，判定为0

if(匹配的值) 

是 则执行

else

否 则执行

switch 寻找正确的值并往下执行



